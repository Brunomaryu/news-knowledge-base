# News Knowledge Base — CLAUDE.md

## システム概要
コンサルタント個人向けニュース知識資産管理システム。業界ニュースを構造化してNotionに蓄積し、業界別・テーマ別・キーワード別に検索・分析できる状態を維持する。

## ユーザープロフィール
- 職種: コンサルタント
- 専門領域: 製造業、AI・データ基盤、経営管理
- 利用目的: 長期的な業界知識の蓄積・構造化・活用

## Notion DB 接続情報

### ニュースDB（既存）
- Data Source ID: `347a6219-f619-80de-a8f9-000b3917fd22`
- Database ID: `347a6219-f619-80ff-bd5c-c42e18612223`
- DB URL: `collection://347a6219-f619-80de-a8f9-000b3917fd22`

### 業界DB（2026-06-28 追加）
- Data Source ID: `7d1b4d5e-18dc-4bd5-bdd2-035e49aaa585`
- DB URL: `collection://7d1b4d5e-18dc-4bd5-bdd2-035e49aaa585`

### 企業DB（2026-06-28 追加）
- Data Source ID: `77e86351-013e-423f-83c6-5851b63f0e21`
- DB URL: `collection://77e86351-013e-423f-83c6-5851b63f0e21`

---

## スキーマ定義（正式定義は `schema/` を参照）

### フィールド一覧

| フィールド名 | 型 | 役割 |
|---|---|---|
| Fact | title | 記事の核心ファクト（1行） |
| 背景 | text | ニュースの背景・文脈 |
| 示唆 | text | コンサルとして読む示唆・論点 |
| 業界 | multi-select | 10カテゴリから選択（複数可） |
| タグ | multi-select | 40タグから選択（複数可）※高市重点(orange)含む |
| 日付 | date | 記事日付（YYYY-MM-DD） |
| Summary | text | 要約（レガシー、任意） |
| 業界／国 | text | レガシーフィールド（参照用） |

### 業界カテゴリ（10分類）
自動車・モビリティ / テクノロジー・AI / 半導体・電子部品 / 素材・資源 / 水処理・環境 / エネルギー / 造船・重工業 / ヘルスケア・流通 / メディア・エンタメ / 政策・マクロ経済

詳細は `schema/taxonomy.md` を参照。

---

## AIの行動ルール

### セッション開始時
1. `schema/taxonomy.md` を参照し、業界・タグの許容値を把握する
2. ユーザーの指示を待つ

### 記事を追加する時
1. `templates/article-input-template.md` のフォーマットで内容を整理する
2. `schema/taxonomy.md` で業界・タグを確認・選択する
3. **登録品質ルール（v1.2.0）**:
   - Factは事実のみ。「〜と思われる」「〜だろう」等の推測表現は背景・示唆に移動
   - タグは最大5件まで（超える場合は優先度の低いものを削除）
   - 出典URLがある場合は「出典URL」フィールドに入力（推奨）
   - 示唆の推奨フォーマットは `templates/insight-template.md` を参照（必須ではない）
4. `notion-create-pages` でNotionに登録する（parent: data_source_id）
5. 追加後、セッションログ用のサマリを手元に控える

### 複数記事を一括追加する時（手書きノート等）
1. 全記事の内容を先に整理・分類する
2. 既存記事との重複確認（`notion-search` で類似タイトルを検索）
3. バッチ単位（5〜8件）で `notion-create-pages` を実行する
4. 完了後、①新規追加件数 ②補完件数 ③主要テーマ をレポートする

### スキーマを変更する時（業界・タグの追加・変更等）
```
① 変更内容・影響範囲をユーザーに提示
        ↓
② ユーザー承認を得る（必須）
        ↓
③ schema/taxonomy.md を更新
④ Notion MCPで ALTER COLUMN を実行
⑤ schema/changelog-schema.md に変更を記録
⑥ CHANGELOG.md に追記
⑦ バージョン更新（必要に応じて）
```

### 既存記事を補完する時
1. `notion-search` で対象記事を検索・IDを特定する
2. `notion-fetch` で現在の内容を確認する
3. 不足フィールド（背景・示唆・日付・タグ等）を特定する
4. `notion-update-page` (command: update_properties) で更新する

### 過去記事を検索・分析する時
1. `notion-search` でキーワード検索する
2. 必要に応じて `notion-fetch` で詳細を参照する
3. 複数記事の比較・分析結果を構造化して提示する

### 週次サマリを生成する時
1. `notion-search` で直近1週間の記事を収集する
2. `templates/weekly-digest-template.md` に従いサマリを生成する
3. `logs/week-YYYY-WNN.md` として保存する

### タグ整合性チェック時
1. `tests/validation_rules.md` のルールを確認する
2. Notionの全記事タグが `schema/taxonomy.md` の許容値内かチェックする
3. 逸脱があれば一覧化してユーザーに報告・修正する

### 既存ニュースから企業・業界DBを一括バッチ生成する時
「ニュースDBから企業DBと業界DBを自動生成して」と指示された場合のフロー：

```
① ニュースDBの全記事を notion-search で収集する（複数回に分けて取得）
        ↓
② 全記事から登場企業を抽出・リストアップする
   - 記事タイトル・本文・背景・示唆から企業名を抽出
   - 重要度・登場頻度でソートし、上位10〜15社を優先対象とする
        ↓
③ 企業DBを notion-search で検索し、既存登録の有無を確認する
        ↓
④ ユーザーに処理計画を提示：
   「以下の企業を処理します：
    - 新規登録：〇〇社（△社）
    - 既存→リンク追加：□□社（△社）
    対象外（マイナー企業等）：〇件
    実行しますか？」
        ↓
⑤ ユーザーが Yes → バッチ単位（4〜5社）で実行
   ├── 新規企業: WebSearch でリサーチ → 企業DBに新規ページ作成 → 関連ニュース記事を紐付け
   └── 既存企業: 関連ニュース記事フィールドに追加リンク
        ↓
⑥ 業界DBのHOTニュースを更新する
   - ニュース記事を業界別に分類（schema/taxonomy.mdの10業界に照合）
   - 各業界DBページに HOTニュース セクションを insert_content で追記
   - 対象業界：該当ニュースが3件以上ある業界を優先
        ↓
⑦ 完了レポートを出力：
   ①新規登録企業数 ②リンク追加企業数 ③HOTニュース更新業界数 ④主要テーマ
```

**バッチ実行の注意点:**
- `notion-create-pages` の parent は `data_source_id` を使う（`database_id` 不可）
- 関連ニュース記事のリレーションはフルURL形式: `["https://app.notion.com/p/{UUID_no_dashes}?pvs=1"]`
- 業界DBへのリレーションも同様のフルURL形式
- 上場区分の正確な表記: 東証プライム / 東証スタンダード / 東証グロース / 未上場 / 海外上場 / その他
- HOTニュース更新のコマンド: `insert_content`（`append` は無効）

---

## 企業DB 運用ルール

### 企業情報の自動生成フロー（ニュース記事登録後）

ニュース記事をNotionに登録した後、以下のフローで企業DBを更新する：

```
① ニュース本文から企業名を抽出する
        ↓
② 企業DBを notion-search で検索し、存在確認する
        ↓
③ 確認メッセージをユーザーに提示する：
   「以下の企業が検出されました。企業DBに追加・連携しますか？
    - 〇〇株式会社（新規）
    - △△社（既存：関連記事として紐付け）」
        ↓
④ ユーザーが Yes → 実行
   ├── 新規企業: WebSearch でリサーチ → 企業DBに新規ページ作成
   └── 既存企業: ニュース記事を関連ニュース記事フィールドに紐付け
```

### 企業情報フォーマット（自動生成時の出力仕様）

**1. ビジネスモデル・沿革（300〜500字）**
ストーリー形式で以下を含める：
- 現在の主力事業・製品・サービス
- 主要顧客・強み・競争優位
- 国内外の事業展開・生産拠点
- 現在注力している成長領域

**2. 社史要約（約200字）**
創業→成長経緯→転換点→現在を概要で記述。
年表形式は避け、企業の歴史が理解できる文章とする。

**3. 基本情報（各フィールドに格納）**
- 企業名 / 業界（リレーション） / 本社所在地 / 設立年
- 代表者 / 売上高（直近決算） / 従業員数 / 上場区分
- 主力製品・サービス（3〜5項目） / 主要顧客 / 主な拠点

**4. 営業利益率推移フォーマット（必須）**
1年1行で記載する（「/」区切り不可）。
```
FY2021: X.X%
FY2022: X.X%
FY2023: X.X%
FY2024: X.X%
FY2025: X.X%（予想）
```

**5. 自分のメモ・学び（示唆の自動引用）**
関連ニュース記事を紐付けた際、各記事の「示唆」フィールドを以下のフォーマットで追記する：
```
📰 記事タイトル
日付: YYYY-MM-DD
リンク: https://app.notion.com/p/{UUID_no_dashes}?pvs=1
示唆: （示唆の本文）

---
```
複数記事がある場合は「---」区切りで連結する。記事更新時も同様に追記する。

**出力方針:**
- 最新情報・直近決算ベース
- 事実ベース（推測不可）
- コンサルタントが1〜2分で企業概要を把握できる粒度

### 企業情報の手動追加時
「〇〇社の情報を企業DBに追加して」と指示する。
AIがWebSearchでリサーチし、フォーマットに沿ってNotionに登録する。

### 既存企業情報の更新時（擬似Draftフロー）
「〇〇社の情報を最新化して」と指示された場合、以下のフローを必ず踏む：

```
① notion-fetch で現在の企業情報ページを取得し、チャットに現在の内容を表示する
        ↓
② WebSearch で最新情報を取得し、「変更前 → 変更後」の差分を提示する
   例: 「売上高: 2.3兆円（FY2023）→ 2.8兆円（FY2024）に更新します」
        ↓
③ ユーザーが承認（Yes） → notion-update-page を実行
   ユーザーが修正指示 → 差分を修正してから実行
```

---

## 業界DB 運用ルール

### 業界情報の自動生成フロー（ニュース記事登録後）

ニュース記事をNotionに登録した後、以下のフローで業界DBを更新する：

```
① ニュース記事から業界を判定する（schema/taxonomy.mdの10業界に照合）
        ↓
② 業界DBの該当ページを notion-search で取得し、「業界概要」が未入力か確認する
        ↓
③ 未入力の場合、確認メッセージをユーザーに提示：
   「〇〇業界の基本情報がまだ登録されていません。WebリサーチしてDBを生成しますか？」
        ↓
④ ユーザーが Yes → WebSearch でリサーチ → 業界DBページを更新
```

### 業界情報フォーマット（自動生成時の仕様）

**プロパティ（Notion一覧画面に表示）:**
- 業界名（既存）
- 業界概要: 300〜500字のストーリー形式（役割・ビジネスモデル・プレイヤー・市場環境・成長領域）
- 世界市場規模: 直近数値（例: 約5,000億ドル・2024年）
- CAGR: 年平均成長率（例: 7.2%・2024-2029年予測）
- DX成熟度: 高・中・低から選択
- 最終更新日: WebSearch実施日

**ページ本文（クリックして開くと見られる詳細情報）:**
以下の構造でMarkdownとして書き込む。

```markdown
## 業界の歴史・変遷
（約200字：誕生→成長要因→転換点→現在）

## 市場規模・成長性
- 世界市場規模: 
- 国内市場規模: 
- 直近10年推移: 
- CAGR: 
- 今後3〜5年予測: 
- 成長要因: 
- 縮小要因: 
※無料公開情報ベース。精度が低い場合は出典を明記。

## 業界構造・主要プレイヤー
（業界構造の概要、国内外主要企業と特徴・強み1〜2行、市場シェア、競争構造）

## バリューチェーン
（上流〜下流の流れ、各工程の役割・収益源・付加価値が高い工程）

## 現在の主要トレンド（5〜10項目）
- **テーマ名**: 概要1〜2行
- ...

## HOTニュース（直近5〜10件）
- **YYYY-MM-DD｜タイトル**
  概要（2〜3行）／業界への影響

## 業界課題・リスク
- **課題名**: 概要1〜2行
- ...

## DX・AI活用動向
- DX成熟度: 高/中/低
- AI活用事例: 
- データ活用状況: 
- 主なDXテーマ: 
- 今後期待される技術: 

## 今後3〜5年の展望
（200〜300字：市場見通し・成長ドライバー・リスク要因・注目技術・構造変化予測）
```

**出力方針:**
- 最新情報を優先し、信頼できる情報源ベースで事実のみ記載
- 市場規模・成長率は可能な限り数値で示す（取得不可の場合はその旨明記）
- 無料公開情報の限界がある場合は出典を明示
- コンサルタントが5分で業界全体像を把握できる粒度

### HOTニュースの更新コマンド
「〇〇業界のHOTニュースを更新して」と指示する。
AIがWebSearchで直近の重要ニュース5〜10件を取得し、ページ本文のHOTニュースセクションを上書き更新する。

### 既存業界情報の全体更新時（擬似Draftフロー）
「〇〇業界の情報を最新化して」と指示された場合、以下のフローを必ず踏む：

```
① notion-fetch で現在の業界ページを取得し、主要データ（市場規模・CAGR・最終更新日）を表示する
        ↓
② WebSearch で最新情報を取得し、変更予定箇所を差分形式で提示する
        ↓
③ ユーザーが承認 → notion-update-page（replace_content または update_properties）を実行
```

---

## 定期レビューサイクル（v1.2.0追加）

| サイクル | タイミング | 実施内容 |
|---|---|---|
| **週次** | 毎週末 | バリデーションR-01〜R-09の実行・週次サマリ生成 |
| **月次** | 月初 | 業界DB情報ステータス確認（R-12）・3ヶ月未更新業界の洗い出し |
| **四半期** | 3ヶ月毎 | 企業DB全社の決算情報確認（R-11）・obsolete判定 |

**週次実行コマンド例:**
```
「今週のニュースDB整合性チェックをお願いします」
「今週の週次サマリを作成してください」
```

---

## ファイル更新ルール

| イベント | 更新ファイル |
|---|---|
| 記事追加（10件以上） | logs/{date}_{topic}.md |
| スキーマ変更 | schema/taxonomy.md + schema/changelog-schema.md + CHANGELOG.md |
| 機能追加・変更 | CHANGELOG.md + REQUIREMENTS.md + VERSION.md |
| 週次サマリ | logs/week-YYYY-WNN.md |

---

## 不変ルール
- タグ・業界の値は `schema/taxonomy.md` の定義外を使わない
- **タグは1記事最大5件まで**（超える場合は重要度の低いものを削除）
- **Factフィールドは事実のみ**（推測・主観表現を含めない）
- スキーマ変更は必ずユーザー承認後に実行する
- **企業DB・業界DBの更新前に必ず現在の内容を表示し、差分をユーザーに確認する**
- Notion MCPツール呼び出しは `notion-create-pages` の parent に `data_source_id` を使う（`database_id` ではない）
- multi-selectの値はJSON配列文字列として渡す（例: `'["生成AI", "OpenAI"]'`）

---

## commit message形式（GitHub連携時）

```
{type}({scope}): {内容（日本語可）}

type: feat / fix / docs / refactor / chore
scope:
  claude    - CLAUDE.md
  schema    - schema/配下
  ops       - OPERATIONS.md
  req       - REQUIREMENTS.md
  template  - templates/配下
  log       - logs/配下
  design    - docs/DESIGN.md
```
